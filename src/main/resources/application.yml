spring:
  application:
    name: back-archetype
    version: 1.0.0-SNAPSHOT

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

info:
  project:
    name: ${spring.application.name}
    version: ${spring.application.version}
